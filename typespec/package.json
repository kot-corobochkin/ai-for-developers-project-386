{
  "name": "calendar-service-typespec",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "./node_modules/.bin/tsp compile .",
    "format": "./node_modules/.bin/tsp format '**/*.tsp'",
    "check": "npm run format -- --check && npm run compile"
  },
  "dependencies": {
    "@typespec/compiler": "latest",
    "@typespec/http": "latest",
    "@typespec/openapi3": "latest",
    "@typespec/versioning": "latest"
  }
}
